# Learnings: components-py

Accumulated craft lessons applied on every run.

<!-- 2026-07-21 · job 720008 · client: Ibby -->
- The library still has no helpers for the designed/third-party families it is increasingly asked to build (two-column VS wrapper, caption-chip with auto-shadow and wrap, price/FREE badge, offer-poster block stack, tombstone plaque). Each batch hand-authors them. A handful of reusable helpers would cut per-batch build time and reduce the layout bugs (right-edge overflow, dead-space, CTA clipping) that the visual QA loop currently has to catch by eye.
