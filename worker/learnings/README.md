# Worker Learnings

Craft lessons the local worker accumulates from client work. One file per
skill (e.g. `avatar-extraction.md`). The worker injects every file here into
each foundation run and appends new lessons with a date + job + client stamp.

These are OUR improvements layered on top of the agency skills — the synced
claude.ai skills cache itself is never modified.
