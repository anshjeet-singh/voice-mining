# Learnings: skool-community-builder

Accumulated craft lessons applied on every run.

<!-- 2026-07-09 · job 60001 · client: Trent Kus -->
- The skill's own name and header brand it "Full Stack Agency," but the mother skill (client-onboarding-orchestrator) mandates that the agency is "Cashflow Coaches" and that "Full Stack Agency" must never appear in any deliverable. Update the skill's branding to Cashflow Coaches (or add a note that the internal name is legacy and never ships) so a writer following this skill doesn't leak the wrong agency name.
- FULL BUILD produces one community per run, but many client funnels need a matched pair: a free top-of-funnel community and a paid delivery community for the offer. The skill treats free-vs-paid only as a VSL variable. Add an explicit "dual build" note covering how to name the pair so ascension reads cleanly, how to differentiate their About pages (free sells one action, paid sells the offer/price/guarantee), and how to link the free community's ascension line to the paid group.
- The membership-question segmenter options are written for a pre-funding credit lead. For a PAID membership the segmenter should split by in-program stage (e.g. repair stage, close-to-fundable) rather than journey-to-entry. Add one line telling the writer to re-cut the segmenter options to the community's tier and the member's actual state on entry.
- Section 4 hands the VSL to `vsl-and-sales-page-writer`, but that skill is not always in scope for the Skool stage (it is a Step 1/Step 3 child in the orchestrator). State that when the handoff target is unavailable, this skill writes the VSL directly from the archetype brief here and still runs the humanizer/dimensionalization chain, so the step never stalls.

<!-- 2026-07-09 · job 90001 · client: Trent Kus -->
- The Section 3 About-page template hardcodes a "⚡ Welcome to [Community Name]" opener, but the appendix instructs the writer to pick the opening move that fits the client (identity / problem-agitation / guarantee / proof / mechanism / mission). These conflict. State that the fixed template skeleton is a fallback only, and the appendix's opening-move selection overrides the default "Welcome" line, so the strongest hook (a hard proof line for a paid page, problem-agitation for a credit free page) leads instead of a generic greeting.
- In a paired free-plus-paid build, the "Start Here" pinned post's first action differs by tier and should not be copied verbatim across both. Free = "Start Here & Book Call"; a paid delivery community's first action is usually an onboarding/setup step (here, "Set Up Monitoring"), not a call. Add a note that the first pinned post's action is retargeted to the tier's actual first step, while Steps 2 and 3 (Introduce Yourself, Accountability Buddy) stay fixed.

<!-- 2026-07-09 · job 90002 · client: Trent Kus -->
- Section 6 (gamification) says Level 1 is Caution and Levels 2 to 9 "progressively reflect growth," but it never states the cross-community rule that a paired free-plus-paid build requires. Add it: in any matched free-plus-paid build, the 1 to 9 level ladder must be IDENTICAL across both communities (one identity system, derived from the client's brand language), so a member ascending from free to paid keeps climbing the same ladder instead of resetting. Without this the skill produces two different ladders and the ascension reads as two disconnected journeys.
- Section 4 (About-page VSL brief) sets tone and archetype but gives no length target, so free VSLs get written too short (under 2 minutes) and paid ones drift long. Add the target from the winning-community data: about 3 minutes spoken, roughly 420 to 480 words, for BOTH the free and paid About-page VSL, compressing beats rather than skipping them. A stated word-count band keeps the two pages congruent and stops the free VSL from underselling.

<!-- 2026-07-11 · job 270011 · client: Trent Kus -->
- The skill produces launch assets only (name, tagline, About page, VSL, categories, level names, plugins, and the three fixed pinned posts) but has no mode or templates for ongoing / on-demand community CONTENT posts. A batch of value, engagement, win/proof, and DM-trigger posts is a recurring deliverable and currently has to be modeled cold from the research report's proven-posts section. Add a "content posts" mode covering the four post types, the comment-keyword DM-workflow skeleton (keyword, first message, qualification exchange, booking push to the funnel link), the one-win-per-asset proof-gate discipline, and the rule that every post must reference the community's real pinned content and its 1-to-9 level ladder so the batch reads as one build with the community. This is true for every client, not this one's taste.

<!-- 2026-07-12 · job 270018 · client: Trent Kus -->
- A pure value/nurture Skool post is not always internal-only. When the generation request names a specific offer as the target (high ticket, paid community, or a lead magnet), even value/nurture posts carry exactly ONE soft CTA to that offer's correct destination, kept value-first and non-pushy. The "value posts point only at internal assets, only proof/DM posts push the offer" rule is not universal; the request's named destination governs. Encode the routing table in the content-posts mode (high ticket to the VSL/booking link, paid community to the paid join link, free/no offer to internal assets or a soft lead-magnet nudge).
- For a multi-post value/nurture batch, run the set as a single SPIN monologue across the pieces (Problem, then Implication grown with real numbers, then Need-payoff the reader states themselves) rather than three interchangeable standalone tips posts, and map one post per step of the offer's named mechanism so the batch reads as one build. This is true for any client whose offer has a named 2-to-4-step mechanism.

<!-- 2026-07-13 · job 360001 · client: Blake Matthews -->
- The Skool About field has a hard character cap (~1000 characters), but Section 3 treats the About page as one unbounded asset. When the long-form About with a value stack, guarantee, FOR/NOT-FOR, and CTA exceeds the cap, deliver it as two sequential under-1000-character pages: Page 1 = opening hook + who-it's-for + the value stack; Page 2 = proof + guarantee + FOR/NOT-FOR + urgency + CTA. Each page must stand alone and end on its own CTA. Add this cap and the two-page split pattern so writers stop handing over a single About that overflows the field. This is true for every client, not this one's taste.

<!-- 2026-07-13 · job 360002 · client: Blake Matthews -->
- The gamification section (levels 1 to 9) documents "Level 1 = Caution, 2 to 9 progressive" but does not state the hard rule that in a paired free plus paid build the 1 to 9 ladder must be IDENTICAL across both communities (one shared identity arc so a member ascending free to paid keeps climbing). That rule currently only lives in skool-about-vsl.md and the learnings, so a builder reading skool-community-builder alone will make two mismatched ladders. Add the identical-ladder rule to section 6.
- The About-page skeleton hardcodes "⚡ Welcome to [Community Name]" as the opener while the appendix says to lead with the strongest opening move (identity / problem-agitation / guarantee / proof / mechanism / mission). The default line reads as the recommended opener and gets shipped as a bland greeting. Demote it explicitly to a fallback only, subordinate to the opening-move selection.
- The proven free-community format uses green ticks (✅) ONLY on the "what you unlock for free" value list and the gift-box (🎁) on every bonus line (weekly Q&A, 1-on-1 onboarding call, community access). This emoji convention converts and is enforced by the orchestrator but is not written into skool-community-builder. Document it in sections 3 and the free-community appendix so it is not lost when the skill runs standalone.

<!-- 2026-07-13 · job 360002 · client: Blake Matthews -->
- When a matched free-plus-paid pair is produced in SEPARATE parallel runs (not one dual build), the "identical 1-9 gamification ladder" rule has no shared source of truth, so each run invents its own ladder and they diverge (this build inherited a free draft laddered Researcher/First Sales/Recruiter/6-Figure Op/The Exit against a paid draft laddered Come-Up/Keys/Affiliate/Operator/Rainmaker/Flywheel/Seven Figures/Family Tree Changer). State that the ladder must be pinned to a single canonical source, the client's brand-vocabulary identity arc from the Brand & Positioning doc, and that each run derives the ladder from that source rather than from the sibling community's draft, so the two converge without cross-run coordination.
- The Section 3 About-page template hardcodes "✅ This is FOR ... / ❌ This is NOT for ...", but the house rule reserves the green tick (✅) as the value-stack signpost. Using ✅ on the FOR line dilutes that signal. State that the green tick belongs only to the what-you-get / value list, and the FOR line uses a neutral marker (or none) with the ❌ kept for the NOT-FOR line, so ✅ stays an exclusive value cue.

<!-- 2026-07-14 · job 390001 · client: Blake Matthews -->
- For a PAID low-ticket community whose value proposition is an income mechanic (affiliate income, funding referral income, reseller income), the four-liner Format B tagline converts best when each of the first three lines is the member's own concrete ACTION and PAYOFF (do X / get placed or unlocked / make $Z per month), then the CTA line is "JOIN FOR $price." The current Format B examples lean on abstract offer nouns ("Get $100K in 0% funding"); an operator on this build rewrote the tagline to lead with the member's action ("GET PAID TO POST / GET PLACED ON BRANDS / MAKE $10K/MO / JOIN FOR $97/MONTH") and it was preferred. State the action-and-payoff-per-line pattern as the default for income-mechanic paid communities so writers do not default to feature-description lines.
- The paid-community value-stack swipe (skool-paid-about-swipe.md) and the About-page examples use em dashes throughout their ✅ and 🎁 lines, but Cashflow Coaches deliverables enforce "no em dashes anywhere." When these swipes are reproduced inside a governed deliverable, every "—" in a stack line must convert to a colon or comma. Note this conversion in the skill so writers do not paste the swipe verbatim and violate the house formatting rule.

<!-- 2026-07-14 · job 390001 · client: Blake Matthews -->
- For a low-ticket paid community (the $97-tier peptide affiliate community), the operator rejected a descriptive prose tagline in favor of a stacked outcome ladder: mechanism line, placement/benefit line, income outcome line, then price + join CTA (e.g. "GET PAID TO POST / GET PLACED ON BRANDS / MAKE $10K/MO / JOIN FOR $97/MONTH," each with a leading emoji). Systemic pattern: a paid-community tagline/hero converts better as a 3-4 line emoji-led outcome stack ending on price + the join action than as a single descriptive sentence. Any income line in a regulated niche still carries its inline "real result, not typical" tag.

<!-- 2026-07-14 · job 420001 · client: Trent Kus -->
- Gamification level names must be EMOJI + one word (max two), and that word must be relevant to the client's actual industry/niche, not a generic climb word. Operator-flagged as a permanent systemic preference: keep the 9-level ladder identical across the free and paid communities, but the label on each level is one niche-specific word behind the emoji.

<!-- 2026-07-14 · job 420001 · client: Trent Kus -->
- Gamification level names must be emoji + ONE word (1 to 2 words absolute maximum), and the single word must be relevant to the client's actual industry or niche, not a generic status word. The operator rejected multi-word, off-niche level names.
- The paid community About page is a value-stack offer page in a fixed shape, reproduce it exactly: hook number headline, an early-bird price line, a proof line, the mechanism in one beat, a ✅ value stack where every line ends in a ($ dollar anchor), 🎁 bonuses each with a ($ value), a 🏷️ Total Value line set against the actual price, a Book A Call CTA, then a 🔥 ANNUAL PLAN block with its own ✅ stack and a 💥 JOIN NOW close.
- Keep both free and paid community copy vibrant, high-energy, and benefit-driven. A flattened, generic rewrite is a rejection trigger. No em dashes anywhere, including titles.

<!-- 2026-07-14 · job 420001 · client: Trent Kus -->
- Gamification level names must be emoji + exactly ONE word, and that word must be specific to the client's industry or niche, not a generic ladder word. Keep the same 9 levels identical across the free and paid communities. (Operator flagged this as a permanent, every-client rule.)
- Keep community copy vibrant, high-energy, and benefit-driven; do not flatten it on a polish pass. A weak offer or tagline is a rejection trigger even when the structure is correct.
- The paid About page follows the value-stack offer format: a hook-number headline, an early-bird price line, a proof line, the mechanism, a ✅ value stack where every line ends in a ($ anchor), 🎁 bonuses with ($ values), a 🏷️ Total Value vs the actual price, a Book A Call CTA, then a 🔥 ANNUAL PLAN block with its own stack and a 💥 JOIN NOW close.
- The free About page opens on a real hook (never "anyone who wants X"), puts the founder credibility line near the top, lists what they unlock for free with a ✅ green-tick list that names exactly three lead magnets, adds 🎁 gift-box bonuses, states one "✅ This is FOR ..." sentence and one "❌ This is NOT for ..." sentence, and ends on a command-close CTA. Its single job is to move people toward the paid offer and a booked call.

<!-- 2026-07-14 · job 420003 · client: Blake Matthews -->
- The PAID community About page must be produced in the approved value-stack offer-page format, not a descriptive benefit paragraph: a hook number headline, an early-bird price, a proof line, the mechanism, a checkmark value stack where every line ends in a ($ anchor), gift-box bonuses with ($ values), a Total Value figure versus the price, a Book A Call CTA, then a separate annual-plan block with its own stack and a JOIN NOW close. This is a systemic operator standard, true for every client.
- Keep community copy (free and paid) vibrant, high-energy, and benefit-driven. Do not flatten it into neutral descriptive prose. The free community mirrors the same voice and pillars in a non-value-stack format whose only job is to move people toward the paid offer and a booked call.

<!-- 2026-07-14 · job 420003 · client: Blake Matthews -->
- Weak or flat community copy is a rejection trigger even when the structure is correct. A previous free/paid community build was rejected specifically because "the offer and tagline were weak." Both free and paid copy must stay vibrant, high-energy and benefit-driven. Never flatten the offer into descriptive feature-sentences; lead with the member's concrete action and payoff.
- The PAID community About page must be authored in the value-stack offer-page format, not prose: (1) a hook-number headline, (2) an early-bird price line, (3) a proof line, (4) the mechanism in one beat, (5) a ✅ value stack where every single line ends in a ($ dollar anchor), (6) 🎁 bonuses each with a ($ value), (7) a 🏷️ Total Value line set against the actual price, (8) a Book A Call CTA, (9) a 🔥 ANNUAL PLAN block with its own ✅ stack ending in "Lifetime Price Locked In", and (10) a 💥 JOIN NOW close.
- The PAID tagline is a punchy four-line emoji format where the first three lines are the member's own action-plus-payoff (get paid to post / get placed on brands / make $X per month) and the last line is the join command with the price ("JOIN FOR $97/MONTH"). Do not default to abstract feature-noun taglines.
- The FREE community About page follows the free format, not the paid value stack: open on a real hook (never "anyone who wants X"), put the founder credibility line near the top, give a ✅ green-tick list of exactly three named lead magnets ("what you unlock for free"), 🎁 gift-box bonuses (weekly live Q&A, a 1-on-1 onboarding call, community access), one "✅ This is FOR ..." sentence and one "❌ This is NOT for ..." sentence, and a command-close CTA. Its single job is to move people toward the paid offer and a booked call.
- The FREE tagline is the same punchy four-line emoji format selling the dream, but the last line is always a "👇 Join below" style command, never a price.
- No em dashes anywhere, including titles and inside the ✅ / 🎁 lines. When a swipe template shows an em dash, convert it to a colon or comma.

<!-- 2026-07-14 · job 420003 · client: Blake Matthews -->
- Build the PAID community About page as a value-stack offer page, never a descriptive paragraph. Required shape: a hook-number headline, an early-bird price line, a one-line social-proof stat, the founder credibility + named mechanism, a ✅ value stack where every included asset is named and ends in a ($ dollar anchor), 🎁 bonuses each with a ($ value), a 🏷️ Total Value line summed against the actual price, a "Book A Call" CTA, then a separate 🔥 ANNUAL PLAN block with its own stack and a 💥 JOIN NOW close. A benefit-sentence tagline or a plain descriptive About page is a reject. This is systemic operator preference, true for every client.
- Build the FREE community About page in the free format (not a paid value stack): open on a real hook (never "anyone who wants X"), a founder credibility line near the top, a ✅ green-tick list of what they unlock for free including exactly three named lead magnets, 🎁 gift-box bonuses (weekly live Q&A, a 1-on-1 onboarding call, community access), one "✅ This is FOR ..." sentence and one "❌ This is NOT for ..." sentence, and a command-close CTA. Its job is to move people toward the paid offer and a booked call.
- Taglines for both communities are the punchy 4-line emoji style selling the dream (get paid to post, get placed on brands, faceless, real money): the paid tagline closes on the price/join line, the free tagline closes on "👇 Join below".
- Keep community copy vibrant, high-energy, and benefit-driven. Do not flatten it into neutral corporate description; a flat, weak tagline or offer is the most common reject on these docs. No em dashes anywhere.
