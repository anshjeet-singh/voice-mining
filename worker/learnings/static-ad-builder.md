# Learnings: static-ad-builder

Accumulated craft lessons applied on every run.

<!-- 2026-07-10 · job 180009 · client: Trent Kus -->
- The skill documents only the SVG + rsvg-convert + components.py pipeline, but the live agency build route is the ad-factory HTML pipeline: one HTML file of `.s` design elements at 360x450, rendered to 1080x1350 via puppeteer (`render.js`, deviceScaleFactor 3), plus `genimage.js` (Nano Banana Pro) for photo-real formats. Worth documenting as the supported alternative so a builder does not have to reverse-engineer it.
- Add a hard warning about CSS class collisions when many `.s` ads live in one file: an unscoped utility class reused across ads (e.g. a generic `.big` used both as a full-ad wrapper and as an inline number style) bleeds background, padding, and flex onto the wrong element and silently overflows the crop. Rule: scope every per-ad class under that ad's id or a unique prefix, and pixel-audit each render for right-edge clipping.
- For lifestyle / photo + caption-chip formats, recommend generating the photo with genimage (text-free) and compositing the caption chips as HTML over a `file://` background, then rendering. Baking caption text into the image model is unreliable; the HTML overlay guarantees text fidelity, which the format needs.

<!-- 2026-07-10 · operator rejection · client: Trent Kus · batch AdsBatch_2026-07-10 -->
- Operator graded the batch 5/10 and rejected it. The four failures, now codified as hard gates in frameworks/static-render-rules.md: (1) hand-rolled HTML chrome instead of this skill's own pipeline (scripts/ + assets/chrome real-device crops + fonts): for covered formats the skill's pipeline is mandatory, ad-factory is fallback only; (2) designed colored CTA text ("link below" in brand green/blue) baked onto native formats: CTAs live ONLY in the format's designated CTA slot (marker line, final chat bubble, last caption chip) styled per spec; (3) an AI-generated person stood in for the founder on the lifestyle ad: never generate humans, real client photos or a no-person composition only; (4) a tweet ad shipped with a dead white void because no one looked at the render: the visual QA loop (render, VIEW the PNG, grade against reference screenshots and the native test, fix, re-render) is mandatory per ad.

<!-- 2026-07-10 · job 210001 · client: Trent Kus -->
- The QA step (Step 4) checks horizontal extents (`qa_extents`, nothing past x=1012) but has no check for VERTICAL overflow. Fixed-height phone-native formats (iMessage, WhatsApp, IG DM) with `overflow:hidden` silently clip the last message or the footer with no error, dropping the closing beat. Add an explicit clip check to the QA loop: render, then confirm the last authored element is fully visible, or use flow layout that grows the canvas instead of a hard-coded height.
- The genimage guidance for lifestyle/story formats should state plainly: environment-only, no person, no figures, no silhouettes. The image model inserts a human by default even when the brief says "founder from behind," which collides with the "NEVER AI-generate a human" red line. Reserve any human founder shot for the client's real supplied photo, and spec the AI-generated frame as objects and environment only.

<!-- 2026-07-11 · job 210003 · client: Trent Kus -->
- Chrome compositing is silently reversible by any later render. `render_all()` regenerates every PNG in `WORKDIR` from its SVG, so if a second generator script (e.g. a non-Notes `gen_native.py`) calls `render_all()` after `gen_notes.py` has already run `compose_chrome()`, the real device chrome is stripped back off the Notes PNGs and they ship as bare white/black notes. This is easy to miss because the SVG never changed. Fix options, any one: (a) have `compose_chrome()` write to a distinct `*_final.png` name instead of overwriting the render, (b) make `render_all()` skip files recorded in a "chrome already composited" manifest, or (c) add a hard warning in SKILL.md Step 4/5 that `compose_chrome()` must be the LAST operation to touch every Notes PNG, after ALL `render_all()` calls across every generator in the batch. A one-line note in the SKILL would have prevented a re-composite pass this run.
- `notes_v2.py` resolves its fonts/chrome via `_HERE = os.path.dirname(os.path.abspath(__file__))`, which breaks when the module is imported from a copied/relocated build dir; the prior client run had to ship a `notes_v2_local.py` shim that hardcodes the absolute skill path. Consider resolving the assets path from the installed skill root (or an env var) so callers can import the real `notes_v2` from any working directory without a shim.
- The visual QA loop is the one rule that catches font-metric wrapping bugs: a hand-built ChatGPT-answer fallback format clipped its inline step lines off the right edge on first render, and only viewing the PNG surfaced it. Worth adding an explicit line to the QA section that any NON-skill fallback format (ad-factory / hand-built SVG) must be width-audited with `text_w()` before its first render, since those formats do not get the components' built-in extent helpers for free.

<!-- 2026-07-11 · job 240001 · client: Trent Kus -->
- A visible, high-contrast CTA is not optional on any native format that has a CTA slot (Reminders, Email, Notes, chat threads). A missing CTA, or a CTA rendered in low-contrast grey, is an operator-level rejection ("an ad without a proper CTA = burning money," "that greyed out text is not eye catching, what's the point"). Render the CTA in the format's own accent (iOS link blue, the marker line) so it reads as a real tappable action, not a caption.
- When a format imitates a recognizable app UI, match the REAL app, not an approximation. A calendar ad must use an authentic Apple or Google Calendar day view (weekday-and-date strip, hour grid, solid event blocks, current-time line), not two floating colored blocks. Half-native UI reads fake and gets rejected; if you cannot get the real UI right, switch formats.
- A proof-gated ad (a [PROOF] screenshot that drops in at launch) must look intentional in its empty state, never a giant dashed placeholder box that dominates the frame. Make the number or the story the hero and keep the screenshot slot a compact, clearly-labeled frame near the foot, so the pre-screenshot render still reads as a finished ad and not a wireframe.
- Do not let a direct-response designed format drift into a corporate or SaaS aesthetic (glossy white cards, pill headers, a pricing-table look). It stops matching the client's own creative language and reads as an obvious ad. For a comparison, default to the handmade whiteboard (marker, red crosses vs green checks, hand-drawn divider) or the native iOS Notes table before a designed card grid.
- Render order for any batch that mixes ad-factory/SVG natives with composited-chrome Notes ads: render the natives first, composite the Notes chrome last. `render_all()` re-rasterizes every SVG in the workdir, so a later native re-render silently strips the chrome that `compose_chrome()` already pasted onto the Notes PNGs.

<!-- 2026-07-11 · job 240001 · client: Trent Kus -->
- Render the on-image CTA in a high-contrast, eye-catching treatment (the blue link-style "Click the link below" the client uses), never greyed-out or low-contrast. A muted CTA that blends into the layout gets flagged as pointless by the operator even when the rest of the ad is approved. Contrast on the CTA is a render-QA gate, not a style preference.

<!-- 2026-07-11 · job 270001 · client: Trent Kus -->
- Search-format ads must match the query to the viewer's awareness level. A branded "is <company> legit / reviews / scam" search is a most-aware prospect vetting a company they already know, so it only works on retargeting. For cold, problem-aware traffic the query must be the pain the prospect actually types (e.g. "why do i keep getting denied for funding"), with the autocomplete rows as adjacent pain searches, and the result block as the solution reveal.
- A search result must carry the full native anatomy or the illusion breaks: favicon, site name, URL breadcrumb, a blue title, then exactly ONE grey description paragraph. Orphaned lines of text floating with no chrome read as a graphic, not a screenshot.
- On notes-app-family formats (Notes, Reminders, checklists), the CTA is handwritten below or outside the app frame, never a fake in-app list item. A checked-circle "Click the link" row breaks the UI because those apps never contain CTAs.
- On any phone-native recreation, a typed blue/underlined CTA makes the creative "look like an ad twice." The CTA belongs either in the caption/primary text or as a handwritten marker scrawl outside the UI, never as designed CTA text floating inside the screenshot.
- "Click the link below" is correct only on native formats where the caption link sits beneath the creative. On a designed DR graphic where the button IS the link, that phrasing reads as confused; use an outcome-worded button label instead.
- When rebuilding to a named handmade reference, match that reference's execution, not just its copy. `mm_var1_napkin_math` is off-white lined notebook paper with every word handwritten (red kicker, equation flow, yellow-underlined green payoff, arrow CTA), not a photoreal napkin with black letterbox bars. `v4_static_editorial` is defined by an editorial serif with an eyebrow (thin rule plus letterspaced caps) and a bordered pill CTA, not bold sans-serif. A comparison ad in a handmade brand system is a marker whiteboard, not a SaaS-style card table.
- Never ship placeholder text baked into a final rendered creative. An on-image line like "screenshot drops in here before launch" means unfinished work was submitted for review, and it leaves the ad with zero proof. Either the real asset is on screen or the concept ships in a format that stands on its own.
- Match reference UI chrome icon-for-icon on branded third-party formats (ChatGPT, iMessage, etc.). Wrong or warped action icons and stray icons not in the reference (e.g. an extra share/upload arrow) get the recreation clocked as fake. Pull the exact icon row and input-bar layout from the reference screenshot.

<!-- 2026-07-11 · job 270001 · client: Trent Kus -->
- On native-format statics (Notes, Reminders, Calendar, iMessage, Google search, ChatGPT) the CTA must be rendered in the format's natural slot, handwritten or native, never typed blue ad-text and never styled as an extra UI element (a fifth checklist item, a Reminders row). A typed CTA inside the app frame reads as "an ad twice" and breaks the native illusion. Pull the CTA out of the app frame and render it in the format's own hand.
- "Click the link below" belongs only on native formats where the caption link sits below the creative. On a designed graphic where the button IS the link, that phrasing reads as confused and should be cut. Match the CTA phrasing to whether the link is in the caption (native) or in the graphic (designed).
- A native UI recreation must reproduce the reference chrome exactly: Google search needs favicon, site name, URL breadcrumb, blue title, then one grey description paragraph; a ChatGPT answer needs the exact icon row, input bar, and header from the reference. Orphaned floating lines with no chrome kill the native illusion and get the creative clocked as an ad. Build every native recreation against its saved reference, pixel-faithful.
- A designed proof graphic that promises a large result in a short timeframe (a big score jump, a fast approval) should be rejected, not revised, when it sits inside a trust-based batch: it reads exactly like the scams the rest of the batch warns against, poisons trust across the whole set, and a specific hard credit/finance number on a stock designed card is a Meta financial-services flag. The salvageable route for proof is the real screenshot dropped into a native format, not a stock navy-card gradient. Match the proof format to the batch's positioning, and never ship a placeholder ("screenshot drops in before launch") inside a final creative.

<!-- 2026-07-11 · job 270002 · client: Trent Kus -->
- Never SVG-trace a UI reaction-icon row (ChatGPT/Claude copy, thumbs-up, thumbs-down, read-aloud, regenerate). Hand-drawn icon glyphs read as fake next to real composited chrome and get rejected on sight. Either composite a real cropped icon strip from the assets, or end the answer clean and let the real input bar plus disclaimer carry the native frame. Cropping a chat screenshot right after the answer text is a normal, native-looking screenshot.
- On a "$0 up front / no fees" style claim, verify it is literally true before baking it onto a static. If the real offer has any start fee, render "small upfront" plus the pay-on-results line, not "$0 up front." A false zero is both a compliance risk and weaker than the honest "small upfront, paid when it works" contrast.

<!-- 2026-07-11 · job 270002 · client: Trent Kus -->
- Decorative app-icon rows and stock brand icons on native "answer engine" and chat formats read as fake and get rejected on sight. Render these formats with the real UI chrome only (compose bar, input field, timestamps) and zero decorative icon strips.

<!-- 2026-07-11 · job 270008 · client: Trent Kus -->
- When cloning a chart-comparison reference for a regulated/financial-services niche, the axis must plot COST or fees, never the outcome amount (funding, revenue, approval). A "our way" line that rises to any dollar level on a dollar axis reads as a stated price or a promised result and trips the "state the model, never the number" rule. Hold it flat at the honest floor (e.g. $0 out of pocket) and let a callout carry the model in words. The only dollar figure should be a citeable competitor fact.
- The DR-family reference templates (three-step-infographic, comparison-table, occupation-callout hero, ui-vs-ui-split) ship with two elements that are publish blockers for any regulated or native-test clone and must be swapped by default: (a) any human figure (illustrated climber, hero cutout) becomes a no-person object/motif, and (b) any faux money notification (Stripe/Shopify payout, sale alert, bank push) becomes a non-monetary status card or a real redacted [PROOF] screenshot. Bake these swaps into the clone step, do not treat them as optional.
- For an on-demand regeneration that says "clone ONLY from categories X, Y, Z and never duplicate an approved ad," the freshest output comes from pairing each named format with an angle the approved native batch did NOT render, not from re-skinning an approved ad in a new format. Read the operator's approved batch first, list which angle-and-format pairings already shipped, then assign each new format an unused angle from the campaign angle matrix.

<!-- 2026-07-11 · operator rejection round 2 · client: Trent Kus -->
- Second batch rejection: 5 on-demand statics (three-step infographic, cost chart, old-vs-new poster, notifications-vs-chat split, napkin) rejected 4-of-5 even though executed cleanly in the Business Belts colorway. Operator verdict: "none of these follow the reference images... so much going on." The one keeper was the phone-native split (notifications vs chat). Codified in frameworks/static-render-rules.md rule 5: batches default to 80%+ PHONE-NATIVE formats; the DR-poster family (posters, infographics, charts) is opt-in by explicit request only. Polish does not compensate for the wrong format family.

<!-- 2026-07-11 · job 270010 · client: Trent Kus -->
- Never ship a rendered static with a `[PROOF: ...]` placeholder, an empty dashed proof box, or any unfinished token baked into the image. A proof slot on a finished PNG holds a real (redacted) screenshot, or the format is not used. Submitting a rendered ad with a visible placeholder reads as unfinished work sent for review.
- Every static must clone a saved reference format from the library (static-render-rules rule 5). Invented templates get clocked as an ad on sight: this run's operator killed a chart on a fintech-dashboard aesthetic, a three-step build on a neon gaming aesthetic, and an old-way/new-way split with anchor-and-rocket clip art, each because it "matched no saved reference." If no reference fits the angle, pick a different format, do not invent one.
- Run a margin-collision QA on lined-paper and margin templates: the ruled margin line must never run through the first glyph of a line. Start all text to the right of the margin rule, and scan the rendered PNG (not just the source) to confirm.
- Kill half-empty layouts. If two thirds of the canvas is empty color blocks (a common failure on split panels and card stacks), tighten the vertical rhythm until the composition feels as full as the reference. Big dead gaps between a payoff line and a footer read as an unfinished ad.
- Keep the CTA convention consistent across a batch. Every ad ends with the batch's standard CTA in the format's native slot; do not break the convention on one ad (a chart-icon CTA when the rest use a plain link line stands out as a mismatch).
- Do not put insider jargon the cold prospect has never heard on the most important element of the ad. Naming a scam-company category ("the mills") on the key data point creates confusion at the exact moment of the pitch. Explain it in plain words or move it.

<!-- 2026-07-11 · job 270013 · client: Trent Kus -->
- Never bake a `[PROOF: ...]` line, a "redacted screenshot at publish" note, or an empty dashed placeholder box into a rendered PNG that gets submitted for review. A placeholder inside a finished static reads as unfinished work and, on a realness-angle ad, leaves the creative with nothing real in it. Gate the ad on the real screenshot before it renders, or drop the proof element entirely and stand the ad on mechanism.
- Every static must clone a saved reference from the winning-ad library (declare `Reference: <filename>`). An invented infographic, dark-fintech-dashboard, or neon-glow template that matches no reference gets clocked as an ad on sight, no matter how clean it is. If an angle has no fitting reference, use the angle that does; do not invent a template to carry it.
- Do not rebuild an angle the batch already covers in a worse or off-brand format. "Duplicate angle, wrong skin" is an automatic reject. Check the approved set before adding a static; if the angle is live in its correct reference style, the new one is redundant.
- Work the whole canvas. Three bullets per side or four cards followed by a large empty colour block is a layout failure ("mostly air", "half the canvas does nothing"). Tighten the vertical rhythm so the page reads full, like the reference.
- On lined-notebook / margin templates, all text must start to the right of the margin rule. A red margin line running through the first letter of each line (and through the "=" of a payoff) reads as broken rendering. Shift text right of the line or remove the line.
- On any chart or graph static, the plotted line must read as a real trend, not a glitch: no vertical spike-then-flat, and nothing may bleed past its own label box or the canvas edge. If the argument is "cost over a process," the line has to visibly move across the process, not jump once and stop.
- Keep the CTA in the batch's standard slot and convention. A CTA with a novel icon (chart, etc.) or a static that just stops with no CTA both break the set. Every ad ends on the format's designated CTA treatment.
- No fake UI or decoration with no referent: a status pill attached to nothing, a bar chart with no axis or meaning, a "file status" card that belongs to no real app. Unanchored UI reads as filler. If a UI element appears, it must belong to a real, recognizable interface.
- Avoid the most generic visual metaphor pairings (anchor-vs-rocket, old-way-vs-new-way split panels with clip art). Combined with a split layout they read as a stock Canva template with the client's words dropped in.

<!-- 2026-07-11 · job 270013 · client: Trent Kus -->
- Never ship a rendered creative for review with a `[PROOF: ...]` placeholder still baked into the image. A placeholder in a finished PNG is unfinished work sent for review. Gate the render: no asset renders until its proof screenshot is dropped in, or the proof slot is cut.
- Every static must match a saved reference format. Invented templates (infographic panels, fintech dashboards, neon-glow gaming aesthetics) get clocked as an ad on sight and fail the native test. If no reference format fits the angle, use the closest saved format, do not invent one.
- Kill dead space. Big empty color blocks, a huge grey block after four cards, two-thirds of a panel blank: if roughly half the canvas does nothing, the layout fails. Tighten the vertical rhythm so the page feels full, like the reference.
- Do not render fake UI with no referent (a "FUNDED" pill attached to nothing, a bar chart with no axis or meaning, a status card belonging to no real app). Fake UI reads as filler and gets clocked as an ad.
- A charted metric must be legible and plot its intended meaning. A line that spikes vertically then runs flat reads as a glitch, and no element may bleed past its label box or off the canvas edge.

<!-- 2026-07-11 · job 270015 · client: Trent Kus -->
- A rebuilt or native ad must be COMPLETE and REAL as rendered. Never let a creative's whole payload be a gated [PROOF] placeholder box (or a fabricated column plus an empty box). A big dashed "[PROOF: ...] screenshot at publish" panel reads as unfinished work sent for review and as "mostly air," and it gets rejected on sight. When the angle is a single proof and the real screenshot is still pending, pick a format that is complete without it (a labelled chat/story recreation that states the result in-copy, a third-party-voice thread), not a placeholder frame.
- CTA presence is format-dependent, and mixing the two inside one batch is what triggers the "this one just stops" rejection. Designed/infographic/poster formats MUST carry a CTA in the format's natural slot. Native screenshot formats (iMessage, Reddit, Notes-with-marker) may ship CTA-free because the chrome has no honest CTA slot. Decide by format, and never leave a designed layout with no CTA while every native beside it also lacks one.
- Invented layouts get "clocked as an ad on sight": a two-column VS infographic, a fintech chart dashboard, a neon three-step card stack, or an anchor-vs-rocket split all read as stock Canva templates even when the copy is right. Only ship a composition that clones an actual saved reference image; when a designed angle keeps failing, move it into a saved native format instead of re-skinning the infographic.

<!-- 2026-07-11 · job 270015 · client: Trent Kus -->
- When a direct-response poster static (chart comparison, three-step infographic, dark designed poster) is rejected, rebuild it as a phone-native format. Do not re-skin the same argument as another poster or chart: a rejected chart redrawn in a new poster style reads as the same "designed ad," fails the native test again, and usually duplicates an angle a native asset already carries. The rebuild is a format change to native, not a restyle.

<!-- 2026-07-12 · job 270019 · client: Trent Kus -->
- When compositing a photo/genimage base into an SVG for rsvg-convert, use a bare `href` on the `<image>` element, not `xlink:href` (an inline `xmlns:xlink` on the child is not reliably honored, and the image silently drops to the background fill). librsvg 2.46+ supports bare `href`.
- The data-URI MIME type must match the ACTUAL image bytes. `genimage.js` frequently writes JPEG bytes to a `.png` filename, so `data:image/png;base64,<jpeg bytes>` fails to decode and the image renders blank at the same PNG file size as the no-image fallback (a silent failure). Re-encode the base through PIL to true PNG bytes before embedding (`Image.open(f).convert("RGB").save(buf,"PNG")`), or sniff the header and set the MIME accordingly.
- Multi-color inline headlines (white/brand-accent words on one centered line) built from `<tspan fill=...>` segments lose their inter-word spaces in librsvg, because whitespace at tspan boundaries is collapsed by default. Add `xml:space="preserve"` to the parent `<text>` (also needed for a red-label-plus-black-body caption line), or the words run together ("Get YourFreeFunding"). This bit two formats this run (lead-magnet headline, cartoon caption).
- `qa_extents()` flags full-bleed DESIGNED formats (photo backgrounds, two-color split panels, edge-to-edge cartoon panels) at max_x 1079 as CHECK, the same false positive already noted for UI chrome. Extend the helper's allowed-exception note to cover intentional full-bleed designed backgrounds and split panels, so a max_x at the canvas edge is not treated as an overflow bug on those formats.
- For a genimage tombstone/graveyard or any "engraved surface" base, prompt for a single central object with a completely BLANK face (explicitly "no text, no engraving, no writing, no logo"), then add the epitaph/label in SVG. This keeps the on-object text compliant (a text-only epitaph, no fabricated brand plaque) and legible, rather than gambling on the image model rendering clean readable text.
- On a designed-family batch that clones references containing an incidental human (the two-panel-cartoon figures), the "never AI-generate a human" red line is satisfied by building the figures as hand-authored vector illustration, which is also the format's stated asset requirement. Note in the skill that "no AI-generated human" does not ban the cartoon-meme format; it bans generating the human, and a drawn vector figure is compliant.

<!-- 2026-07-13 · job 360004 · client: Blake Matthews -->
- scripts/notes_v2.py line ~131 puts a backslash inside an f-string expression (`f'{" font-weight=\"700\"" if bold else ""} ...'`). That is a SyntaxError on Python < 3.12, so `import notes_v2` crashes the entire build on a 3.11 interpreter and forces every run on that machine to ship a patched local copy. Precompute the attribute into a plain variable before the f-string (`wt = ' font-weight="700"' if bold else ''`) so the module imports on 3.11 as well as 3.12+. This is environment-level and true for every client.
- components.py `search_bar()` hardcodes the query at font-size 42 with no width fit, so any query longer than the pill silently overflows past the right edge (hit this run on "how to start a peptide company payment processing"). Either measure the query with the bundled font and auto-shrink/truncate to the pill width, or document a max query length in native-formats.md, so the search format cannot ship a clipped query.
- For hand-built fallback UI formats (Reddit thread, Claude/ChatGPT answer, tweet), the numbered/answer body lines must be width-audited with `text_w()` and wrapped BEFORE the first render, not after. Two of these clipped off the right edge on first render this batch and only the visual QA loop caught them. Worth a one-line reminder in Step 4 that any non-Notes text block wider than the safe zone gets wrapped programmatically, since these formats do not inherit the components' extent helpers.

<!-- 2026-07-13 · job 360005 · client: Blake Matthews -->
- When cloning an AI-chat reference (Claude or ChatGPT), recreate the FULL bottom chrome the reference shows, not just the answer. An answer that ends at the closing line, with a half-built or missing action-icon row and a warped reply bar, gets clocked as a bad recreation. Match the reference's action-icon row (copy, share, play, thumbs up, thumbs down, retry) with clean monoline geometry, and rebuild the reply bar so the "+", the model chip, the mic, and the voice orb all sit aligned on one row.
- Use the reference's own model-chip label verbatim (e.g. "Fable 5 High"), never substitute a different model name onto it. A hand-inserted model string ("Opus 4.8") lands misplaced and reads as an off recreation; cloning the reference's label keeps the chip native and correctly sized.
- Auto-fit any large serif/display headline to the canvas margins before rendering. A bold answer heading at a fixed size silently clips off the right edge; shrink the size until it fits x=60..1020, or the recreation ships with a cut-off word.

<!-- 2026-07-13 · job 360005 · client: Blake Matthews -->
- none

<!-- 2026-07-13 · job 360006 · client: Blake Matthews -->
- Do not bake an unconfirmed brand name, community name, or invented domain into a rendered static (brand marks, SERP result titles, URLs, etc.). If a format element requires a name the client has not locked, drop that element or use the real platform/generic wording rather than inventing one. An assumed name printed on creative is a rejection trigger.

<!-- 2026-07-13 · job 360007 · client: Blake Matthews -->
- On the search / SERP branded-result format (format 10), let the branded answer result's own title carry the full promise on a single line, then hand straight to a short CTA. Do not stack a separate bold summary kicker above the CTA (e.g. "The answer was always obvious." / "The operators already solved this."): when the answer title already states the promise, that kicker restates it and reads as clutter. Auto-fit the answer title down to one line rather than letting it wrap.

<!-- 2026-07-16 · job 510004 · client: Veda Ray -->
- scripts/notes_v2.py still ships the Python 3.11 SyntaxError in rich_line (the `f'{" font-weight=\"700\"" ...}'` backslash-in-f-string, line ~131), so every run on a 3.11 interpreter has to ship a patched local copy before Notes ads can build. This was flagged 2026-07-13 and is still unfixed. Precompute the weight attribute into a plain variable before the f-string in the shipped file so `import notes_v2` works on 3.11 without a shim.
- The Notes marker CTA can render its hand-drawn underline underneath the bottom toolbar chrome when the content block runs tall, which reads as a CTA with no underline (or a missing CTA). The QA loop should check the CTA baseline plus its underline sits clearly above CONTENT_BOTTOM (1175), not only that the content fits the safe zone. Add it as a per-ad Notes clearance check.

<!-- 2026-07-21 · job 720004 · client: Ibby -->
- scripts/notes_v2.py line ~131 uses a backslash inside an f-string expression (`f'{" font-weight=\"700\"" if bold else ""}'`), which only parses on Python 3.12+. On Python 3.11 the whole module fails to import with a SyntaxError, which silently blocks the entire Notes pipeline. Precompute the attribute into a plain variable first (`wt = ' font-weight="700"' if bold else ''`) so the module imports on 3.11 as well. This is environment-systemic, not client-specific.
- The Notes CTA rule mandates the exact phrase "Click the link below to get started" for link funnels, but that string rendered in the Permanent Marker CTA overflows the 1080px canvas at the reference marker size. The marker CTA helper should measure the real font width and auto-scale to fit within the content width (leave ~90px side margins), rather than assuming a fixed size. Applies to every client using the link-funnel Notes CTA.

<!-- 2026-07-21 · job 720006 · client: Ibby -->
When cloning a UI-native format (Claude/ChatGPT chat, iMessage, Reddit, search), matching the reference means matching its STRUCTURE, not just swapping the text into a rough approximation. The Claude ad was rejected for "formatting looks off" because the build ran the body edge-to-edge with no left margin, crowded the answer against the user bubble, used a tight self-invented five-icon row, and left the input bar half-built (no mic, no voice orb). Before delivering any UI clone, checklist against the reference: (1) left/right margins and the gap under the last inbound bubble, (2) list indent and line spacing, (3) the exact count and shape of any action/icon row, (4) every control in the input/reply bar, (5) nothing clipped at the canvas edge. The side-by-side QA in static-render-rules rule 4 must grade these affordances, not only the copy.
